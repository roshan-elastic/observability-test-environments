{%
  set obj = [
    (elastic_stack_es_env | default({})),
    (elastic_stack_kbn_env | default({})),
    (elastic_stack_apm_env | default({})),
    (elastic_stack_fleet_env | default({}))
  ]
%}
{% for item in obj %}
{% for key, value in item.items() %}
{{ key }}={{ value }}
{% endfor %}
{% endfor %}
