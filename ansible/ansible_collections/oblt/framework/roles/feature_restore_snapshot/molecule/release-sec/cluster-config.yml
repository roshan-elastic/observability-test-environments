cluster_name: "oblt-test-molecule-{{ cluster_seed }}"
save_secrets: true
deploy_timestamp: foo #always the same to check the idenpotence.
build_dir: "{{ lookup('env', 'MOLECULE_SCENARIO_DIRECTORY') }}/build"
ephemeral: true
stack:
  mode: "ess"
  template: "observability"
  target: "production"
  ess:
    provider: gcp
    elasticsearch:
      zones: 2
