---
# k8s configmap
#-----------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "integrations.fullname" . }}-init-script
data:
  init.sh: |
    #!/usr/bin/env bash
    mkdir -p ${SHARE_DIR}/packages/development
    mkdir -p ${SHARE_DIR}/logs
