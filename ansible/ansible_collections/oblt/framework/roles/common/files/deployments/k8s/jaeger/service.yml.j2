---
apiVersion: v1
kind: Service
metadata:
  name: jaeger-service
  labels:
    run: jaeger-service
  namespace: "{{ k8s_default_namespace }}"
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: jaeger
  type: ClusterIP
