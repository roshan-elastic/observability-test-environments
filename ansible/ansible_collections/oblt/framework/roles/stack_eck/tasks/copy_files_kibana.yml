---
- name: Generate ECK Kibana config files
  template:
    src: "{{ item.file }}.j2"
    dest: "{{ build_dir }}/{{ item.file }}"
    mode: 0600
  loop:
    - file: "k8s/kibana/kibana-deployment.yaml"
  tags:
    - kibana
    - generate-config-eck
    - molecule-idempotence-notest
