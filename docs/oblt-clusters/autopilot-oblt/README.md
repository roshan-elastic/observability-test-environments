# Overview

This oblt cluster is a simple GKE Autopilot cluster deployment to share k8s resources across other oblt-cluster.
Each oblt cluster must use a different namespace to place its resources.

## Update

There is no updates.
